import 'package:flutter/material.dart';

class GITHUB extends StatelessWidget {
  const GITHUB({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("заебааал гитхаааб"),
    );
  }
}